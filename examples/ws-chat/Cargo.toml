[package]
name = "ws-chat"
version = "0.1.0"
edition = "2021"
description = "WebSocket chat demo for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
