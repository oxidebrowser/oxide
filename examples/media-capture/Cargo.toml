[package]
name = "media-capture"
version = "0.1.0"
edition = "2021"
description = "Camera, microphone, and screen capture demo for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
png = "0.17"
