[package]
name = "events-demo"
version = "0.1.0"
edition = "2021"
description = "Event system demo for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
