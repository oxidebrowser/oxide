[package]
name = "timer-demo"
version = "0.1.0"
edition = "2021"
description = "Timer API demo for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
