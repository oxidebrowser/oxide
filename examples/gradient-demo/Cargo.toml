[package]
name = "gradient-demo"
version = "0.1.0"
edition = "2021"
description = "Gradient showcase for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
